# Loaded by `rspeedy dev`. By default, src/api/config.ts targets the local API
# on port 8080 and uses the dev bundle hostname, so a phone opened from the QR
# calls your Mac's LAN IP instead of the phone's own localhost.
#
# Override here only when you need a specific API host:
# PUBLIC_API_BASE=http://localhost:8080
