// Overwritten by the "Generate DevServerHost" build phase (see project.yml)
// with the Mac's current LAN IP at build time. Committed with a localhost
// default so the project generates on a fresh checkout; don't edit by hand.
let devServerHost = "localhost"
